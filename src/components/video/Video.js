import Title from "../title/Title";
import VideoList from "../videoList/VideoList";

const Video = () => {
  return (
    <div>
      <Title title={"Video"} />
      <VideoList />
    </div>
  );
};

export default Video;
